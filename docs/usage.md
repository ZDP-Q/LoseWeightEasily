# 🍎 使用指南

## 快速入门

### 1. 安装项目

```bash
# 克隆项目
git clone https://github.com/ZDP-Q/LossWeightEasily.git
cd LossWeightEasily

# 安装依赖
uv sync
```

### 2. 首次运行

首次运行时会自动：
1. 导入食物数据到 SQLite 数据库
2. 下载多语言嵌入模型（约 420MB）
3. 构建 FAISS 向量索引

```bash
uv run loss-weight
```

## 使用方式

### 交互式查询（推荐）

```bash
uv run loss-weight
```

进入交互界面后：

```
🍎 食物卡路里查询系统
============================================================
支持中文和英文搜索，输入 'q' 或 'quit' 退出
============================================================

请输入食物名称: 番茄
```

### 命令行单次查询

```bash
# 搜索食物
uv run loss-weight search "番茄"

# 限制返回数量
uv run loss-weight search "beef" -n 5
```

### Python API

```python
from loss_weight import query_food_calories, FoodSearchEngine

# 简单查询（打印结果）
query_food_calories("番茄")

# 获取结构化数据
engine = FoodSearchEngine()
engine.ensure_index()

# 搜索
results = engine.search("tomato", limit=5)
for fdc_id, description, category, similarity in results:
    print(f"{description} (相似度: {similarity:.2f})")

# 搜索并获取完整信息
details = engine.search_with_details("apple", limit=3)
for food in details:
    print(f"{food['name']}: {food['calories_per_100g']} kcal/100g")
```

## 查询示例

### 中文查询

```
请输入食物名称: 番茄

🔍 搜索: 番茄
============================================================

找到 9 个匹配结果 (其中 9 个有热量数据):

1. Tomatoes, grape, raw
   分类: Vegetables and Vegetable Products
   📊 热量: 27.0 kcal/100g
   📏 常用份量:
      • 5.0 tomatoes (49.7g) = 13.4 kcal
      • 1.0 RACC (85.0g) = 22.9 kcal
```

### 英文查询

```
请输入食物名称: beef

🔍 搜索: beef
============================================================

找到 10 个匹配结果:

1. Frankfurter, beef, unheated
   分类: Sausages and Luncheon Meats
   📊 热量: 314.0 kcal/100g
   📏 常用份量:
      • 1.0 piece (48.6g) = 152.6 kcal
```

### 语义搜索

```
请输入食物名称: 坚果

🔍 搜索: 坚果
============================================================

找到 10 个匹配结果:

1. Almonds, dry roasted, salted
2. Walnuts, English
3. Peanuts, Virginia, raw
...
```

## 常见食物查询词

### 蔬菜类
- 番茄/西红柿 → tomato
- 胡萝卜/红萝卜 → carrot
- 土豆/马铃薯 → potato
- 玉米、黄瓜、生菜、菠菜、西兰花等

### 肉类
- 牛肉 → beef
- 猪肉 → pork
- 鸡肉 → chicken
- 热狗 → frankfurter
- 香肠、培根、火腿等

### 坚果类
- 杏仁 → almond
- 核桃 → walnut
- 花生 → peanut
- 腰果、开心果等

### 水果类
- 苹果 → apple
- 香蕉 → banana
- 橙子 → orange
- 葡萄、草莓等

## 命令参考

| 命令 | 说明 |
|------|------|
| `loss-weight` | 启动交互式查询 |
| `loss-weight search <词>` | 搜索食物 |
| `loss-weight init` | 初始化数据库 |
| `loss-weight stats` | 显示统计信息 |
| `loss-weight rebuild-index` | 重建索引 |

## 常见问题

### Q: 首次运行很慢？
A: 首次运行需要下载嵌入模型（约 420MB），请确保网络通畅。

### Q: 搜索结果不准确？
A: 尝试使用更具体的关键词，或尝试英文搜索。

### Q: 如何更新数据？
A: 运行 `loss-weight init --force` 重新导入数据。
